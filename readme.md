# My Python Chat App
A real-time chat application built with FastAPI, WebSockets, and SQLAlchemy.

## How to run locally
1. Install requirements: `pip install -r requirements.txt`
2. Initialize DB: `python database.py`
3. Run app: `uvicorn main:app --reload`